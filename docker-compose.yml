version: '3.8'
services:
  db:
    image: mysql:8.0
    container_name: financio_db
    ports:
      - "3307:3306"
    environment:
      MYSQL_DATABASE: financio_backend
      MYSQL_ROOT_PASSWORD: secret
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: